# ct\_iu\_top 文档格式修复计划

## 修改目标

基于方案一修改 SKILL 定义，调整章节结构，使其更符合实际需求。

## 最终章节结构

| 序号 | 章节名称     | 必需 | 内容说明                       |
| -- | -------- | -- | -------------------------- |
| 1  | 模块概述     | ✅  | 基本信息 + 功能描述 + 参数           |
| 2  | 模块接口说明   | ✅  | 输入/输出端口表格                  |
| 3  | 模块框图     | ✅  | Mermaid 架构图 + PNG + 流水线结构图 |
| 4  | 模块实现方案   | ✅  | 流水线详细说明 + 关键逻辑描述 + 数据前递机制  |
| 5  | 状态转移图    | ⚠️ | 如适用（检测到状态机时）               |
| 6  | 内部关键信号列表 | ✅  | wire/reg 信号表格              |
| 7  | 模块表项设置   | ⚠️ | 如有表项配置                     |
| 8  | 子模块方案    | ✅  | 例化树 + 子模块列表 + 功能说明         |
| 9  | 可测试性设计   | ⚠️ | 如有测试信号                     |
| 10 | 修订历史     | ✅  | 版本记录                       |

## 章节内容调整

### 1. 模块概述

合并原 SKILL 的"模块功能说明"和"模块参数"：

```markdown
## 1. 模块概述

### 1.1 基本信息

| 属性 | 值 |
|------|-----|
| 模块名称 | xxx |
| 文件路径 | xxx |
| 参数 | PARAM1=值, PARAM2=值 |

### 1.2 功能描述

从文件头注释和信号名推断的功能描述。

### 1.3 设计特点

- 特点1
- 特点2
```

### 2. 模块接口说明

保持原有格式，分为输入端口和输出端口：

```markdown
## 2. 模块接口说明

### 2.1 输入端口

| 信号名 | 方向 | 位宽 | 描述 |

### 2.2 输出端口

| 信号名 | 方向 | 位宽 | 描述 |
```

### 3. 模块框图

**新增：将流水线结构图移入此章节**

```markdown
## 3. 模块框图

### 3.1 模块架构图

Mermaid 架构图 + PNG 图片

### 3.2 流水线结构图

Mermaid 流水线图 + PNG 图片（如检测到流水线）

### 3.3 主要数据连线

| 源模块 | 目标模块 | 信号名 | 位宽 | 说明 |
```

### 4. 模块实现方案

**新增：将流水线详细说明移入此章节**

```markdown
## 4. 模块实现方案

### 4.1 流水线设计

#### 4.1.1 流水线概述

| 流水线 | 执行单元 | 级数 | 支持指令 |

#### 4.1.2 各流水线详细说明

- Pipe0 详细说明
- Pipe1 详细说明
- Pipe2 详细说明

### 4.2 关键逻辑描述

从 always 块和 assign 语句提取的关键逻辑。

### 4.3 数据前递机制

| 前递路径 | 源阶段 | 目标阶段 | 说明 |

### 4.4 流水线控制信号

| 信号 | 说明 |
```

### 5. 状态转移图

保持不变，如检测到状态机则生成。

### 6. 内部关键信号列表

```markdown
## 6. 内部关键信号列表

### 6.1 寄存器信号

| 信号名 | 位宽 | 描述 |

### 6.2 线网信号

| 信号名 | 位宽 | 描述 |
```

### 7. 模块表项设置

```markdown
## 7. 模块表项设置

| 表项名称 | 域段内容 | RAM资源 | 说明 |
```

### 8. 子模块方案

**调整：调用 verilog-file-tree，增加例化树图**

```markdown
## 8. 子模块方案

### 8.1 模块例化树

Mermaid 例化层次图

### 8.2 子模块列表

| 层级 | 模块名 | 实例名 | 文件路径 | 功能描述 |

### 8.3 子模块功能说明

各子模块的详细功能说明。

### 8.4 子模块文档链接

- [子模块A 详细文档](./submodule_a.md)
```

### 9. 可测试性设计

```markdown
## 9. 可测试性设计

### 9.1 测试信号

| 信号名 | 方向 | 位宽 | 描述 |

### 9.2 调试接口

调试接口说明。

### 9.3 扫描链支持

扫描链配置说明。
```

### 10. 修订历史

**新增章节**

```markdown
## 10. 修订历史

| 版本 | 日期 | 作者 | 说明 |
|------|------|------|------|
| 1.0 | YYYY-MM-DD | Auto-generated | 初始版本 |
```

## 实施步骤

### 步骤 1：修改 SKILL 定义

编辑 `d:\code\openc910\.trae\skills\verilog-doc-generator\SKILL.md`：

* 更新步骤 4 的章节结构定义

* 添加流水线内容拆分说明

* 添加修订历史章节

* 更新章节顺序

### 步骤 2：重新生成 iu\_top 文档

按照新的 SKILL 定义重新生成文档：

1. 解析 ct\_iu\_top.v 源代码
2. 调用 verilog-block-diagram 生成框图和流水线图
3. 按新章节结构生成 Markdown 文档
4. 生成 Word 文档

### 步骤 3：验证文档格式

检查生成的文档是否符合新的章节结构。

## 文件修改清单

| 文件                                            | 操作   | 说明       |
| --------------------------------------------- | ---- | -------- |
| `.trae/skills/verilog-doc-generator/SKILL.md` | 修改   | 更新章节结构定义 |
| `docs/iu_top.md`                              | 重新生成 | 按新结构生成   |
| `docs/iu_top.docx`                            | 重新生成 | 按新结构生成   |

